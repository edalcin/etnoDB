<section class="card mb-8 border-l-4 <%= evidence.status === 'approved' ? 'border-green-500' : evidence.status === 'rejected' ? 'border-red-500' : 'border-yellow-500' %>" x-data="{ showDeleteModal: false, selectedStatus: '<%= evidence.status %>' }" @open-delete-modal.window="showDeleteModal = true">
  <div class="flex items-start justify-between mb-4">
    <div>
      <h2 class="text-xl font-semibold text-gray-900 mb-2">Status da Evidência</h2>
      <p class="text-sm text-gray-600">
        ID: <code class="font-mono text-xs bg-gray-100 px-2 py-1 rounded"><%= evidence.id %></code>
      </p>
    </div>
    <div>
      <%- include('status-badge', { status: evidence.status }) %>
    </div>
  </div>

  <!-- Status Change Form -->
  <form method="POST" action="/evidence/status/<%= evidence.id %>" class="mt-4">
    <div class="bg-gray-50 rounded-lg p-4">
      <label class="form-label mb-3">Alterar Status:</label>

      <div class="space-y-2">
        <!-- Pending -->
        <label class="flex items-center p-3 border rounded cursor-pointer hover:bg-white transition-colors" :class="selectedStatus === 'pending' ? 'bg-white border-yellow-500 ring-1 ring-yellow-500' : 'border-gray-200'">
          <input
            type="radio"
            name="status"
            value="pending"
            <%= evidence.status === 'pending' ? 'checked' : '' %>
            class="h-4 w-4 text-yellow-600 focus:ring-yellow-500"
            @change="selectedStatus = 'pending'"
          >
          <span class="ml-3 flex items-center gap-2">
            <span class="font-medium text-gray-900">Pendente</span>
            <span class="text-xs text-gray-600">(aguardando revisão)</span>
          </span>
        </label>

        <!-- Approved -->
        <label class="flex items-center p-3 border rounded cursor-pointer hover:bg-white transition-colors" :class="selectedStatus === 'approved' ? 'bg-white border-green-500 ring-1 ring-green-500' : 'border-gray-200'">
          <input
            type="radio"
            name="status"
            value="approved"
            <%= evidence.status === 'approved' ? 'checked' : '' %>
            class="h-4 w-4 text-green-600 focus:ring-green-500"
            @change="selectedStatus = 'approved'"
          >
          <span class="ml-3 flex items-center gap-2">
            <span class="font-medium text-gray-900">Aprovado</span>
            <span class="text-xs text-gray-600">(visível na busca pública)</span>
          </span>
        </label>

        <!-- Rejected -->
        <label class="flex items-center p-3 border rounded cursor-pointer hover:bg-white transition-colors" :class="selectedStatus === 'rejected' ? 'bg-white border-red-500 ring-1 ring-red-500' : 'border-gray-200'">
          <input
            type="radio"
            name="status"
            value="rejected"
            <%= evidence.status === 'rejected' ? 'checked' : '' %>
            class="h-4 w-4 text-red-600 focus:ring-red-500"
            @change="selectedStatus = 'rejected'"
          >
          <span class="ml-3 flex items-center gap-2">
            <span class="font-medium text-gray-900">Rejeitado</span>
            <span class="text-xs text-gray-600">(não será exibido)</span>
          </span>
        </label>

        <!-- Justificativa de Rejeição -->
        <div x-show="selectedStatus === 'rejected'" x-transition class="mt-3 p-3 bg-red-50 border border-red-200 rounded">
          <label class="form-label text-sm text-red-800 mb-2" for="justificativaRejeicao">
            Justificativa da Rejeição <span class="text-red-600">*</span>
          </label>
          <textarea
            id="justificativaRejeicao"
            name="justificativaRejeicao"
            rows="3"
            class="form-input text-sm"
            placeholder="Informe o motivo da rejeição desta evidência..."
            x-bind:required="selectedStatus === 'rejected'"
          ><%= evidence.justificativaRejeicao || '' %></textarea>
        </div>
      </div>

      <div class="mt-4 flex items-center gap-3">
        <button type="submit" class="btn btn-primary">
          Atualizar Status
        </button>
        <button
          type="button"
          class="btn bg-red-600 hover:bg-red-700 text-white"
          @click="$dispatch('open-delete-modal')"
        >
          Deletar Evidência
        </button>
      </div>
    </div>
  </form>

  <!-- Justificativa de Rejeição (exibição permanente) -->
  <% if (evidence.status === 'rejected' && evidence.justificativaRejeicao) { %>
  <div class="mt-4 p-3 bg-red-50 border border-red-200 rounded">
    <p class="text-sm font-medium text-red-800 mb-1">Motivo da Rejeição:</p>
    <p class="text-sm text-red-700"><%= evidence.justificativaRejeicao %></p>
  </div>
  <% } %>

  <!-- Metadata -->
  <div class="mt-4 pt-4 border-t border-gray-200 text-xs text-gray-600 space-y-1">
    <% if (evidence.createdAt) { %>
      <p><strong>Criado em:</strong> <%= new Date(evidence.createdAt).toLocaleString('pt-BR') %></p>
    <% } %>
    <% if (evidence.updatedAt && evidence.createdAt && evidence.updatedAt.toString() !== evidence.createdAt.toString()) { %>
      <p><strong>Atualizado em:</strong> <%= new Date(evidence.updatedAt).toLocaleString('pt-BR') %></p>
    <% } %>
    <% if (evidence.fonte) { %>
      <p><strong>Fonte:</strong> <%= evidence.fonte %></p>
    <% } %>
  </div>

  <!-- Delete Confirmation Modal -->
  <div
    x-show="showDeleteModal"
    x-cloak
    class="fixed inset-0 z-50 overflow-y-auto"
    aria-labelledby="modal-title"
    role="dialog"
    aria-modal="true"
    @keydown.escape.window="showDeleteModal = false"
  >
    <!-- Overlay -->
    <div
      class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity"
      @click="showDeleteModal = false"
      x-show="showDeleteModal"
      x-transition:enter="ease-out duration-300"
      x-transition:enter-start="opacity-0"
      x-transition:enter-end="opacity-100"
      x-transition:leave="ease-in duration-200"
      x-transition:leave-start="opacity-100"
      x-transition:leave-end="opacity-0"
    ></div>

    <!-- Modal Content -->
    <div class="flex min-h-full items-end justify-center p-4 text-center sm:items-center sm:p-0">
      <div
        class="relative transform overflow-hidden rounded-lg bg-white text-left shadow-xl transition-all sm:my-8 sm:w-full sm:max-w-lg"
        x-show="showDeleteModal"
        x-transition:enter="ease-out duration-300"
        x-transition:enter-start="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
        x-transition:enter-end="opacity-100 translate-y-0 sm:scale-100"
        x-transition:leave="ease-in duration-200"
        x-transition:leave-start="opacity-100 translate-y-0 sm:scale-100"
        x-transition:leave-end="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
      >
        <div class="bg-white px-4 pb-4 pt-5 sm:p-6 sm:pb-4">
          <div class="sm:flex sm:items-start">
            <!-- Warning Icon -->
            <div class="mx-auto flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-full bg-red-100 sm:mx-0 sm:h-10 sm:w-10">
              <svg class="h-6 w-6 text-red-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
              </svg>
            </div>
            <!-- Content -->
            <div class="mt-3 text-center sm:ml-4 sm:mt-0 sm:text-left">
              <h3 class="text-base font-semibold leading-6 text-gray-900" id="modal-title">
                Confirmar Deleção
              </h3>
              <div class="mt-2">
                <p class="text-sm text-gray-500">
                  Tem certeza que deseja deletar esta evidência? Esta ação não pode ser desfeita e todos os dados associados (comunidades e plantas) serão permanentemente removidos.
                </p>
              </div>
            </div>
          </div>
        </div>
        <!-- Actions -->
        <div class="bg-gray-50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6 gap-3">
          <form method="POST" action="/evidence/delete/<%= evidence.id %>">
            <button
              type="submit"
              class="inline-flex w-full justify-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 sm:w-auto"
            >
              Deletar
            </button>
          </form>
          <button
            type="button"
            class="mt-3 inline-flex w-full justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50 sm:mt-0 sm:w-auto"
            @click="showDeleteModal = false"
          >
            Cancelar
          </button>
        </div>
      </div>
    </div>
  </div>
</section>
