<div class="text-center py-12">
  <svg class="mx-auto h-24 w-24 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
  </svg>

  <h3 class="mt-4 text-xl font-semibold text-gray-900">
    Nenhuma referência encontrada
  </h3>

  <%
    const hasFilters = filters.comunidade || filters.planta || filters.estado || filters.municipio;
  %>

  <% if (hasFilters) { %>
    <p class="mt-2 text-gray-600 max-w-md mx-auto">
      Não encontramos resultados para os filtros aplicados. Tente:
    </p>

    <ul class="mt-3 text-sm text-gray-700 space-y-1 max-w-md mx-auto">
      <li>• Usar termos mais genéricos</li>
      <li>• Verificar a ortografia</li>
      <li>• Remover alguns filtros</li>
      <li>• Buscar por apenas um campo por vez</li>
    </ul>

    <div class="mt-6">
      <a href="/" class="btn btn-primary">
        Limpar Filtros
      </a>
    </div>
  <% } else { %>
    <p class="mt-2 text-gray-600 max-w-md mx-auto">
      Ainda não há referências aprovadas no banco de dados.
    </p>

    <p class="mt-3 text-sm text-gray-700">
      Use a interface de <a href="http://localhost:3001" class="text-forest-600 hover:text-forest-700 underline">Entrada de Dados</a>
      para cadastrar novas referências.
    </p>
  <% } %>

  <!-- Info Box -->
  <div class="mt-8 max-w-md mx-auto bg-forest-50 border border-forest-200 rounded-lg p-4">
    <h4 class="text-sm font-semibold text-forest-800 mb-2">
      💡 Dica de Busca
    </h4>
    <p class="text-xs text-forest-700 text-left">
      Você pode buscar por nome de comunidade, nome da planta (científico ou vernacular),
      estado ou município. A busca não diferencia maiúsculas de minúsculas e aceita
      correspondências parciais para comunidades e plantas.
    </p>
  </div>
</div>
